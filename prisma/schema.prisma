// TutorConnect — PostgreSQL schema (models added in later modules)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
